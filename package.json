{
  "name": "gg-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Curated ABAP samples for SAP GUI controls and screen elements",
  "scripts": {
    "lint": "abaplint",
    "verify:repo": "node scripts/verify-repo.mjs",
    "test": "npm run verify:repo && npm run lint"
  },
  "devDependencies": {
    "@abaplint/cli": "2.120.33"
  }
}
